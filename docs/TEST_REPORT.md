# 테스트 보고서

검증일: 2026-09-05 KST

## 자동화 검증

### 단위 테스트

명령: `npm test`

결과: **27 passed, 0 failed**

| 대상 | 검증 내용 |
|---|---|
| 정규화 | 플랫폼 원본 필드가 공통 ID, 출처명, 가격, 욕실, 좌표로 변환됨 |
| 복합 필터 | 검색어, 최대 가격, 최소 욕실, 최소 인원, 편의시설이 AND로 적용됨 |
| 편의시설 경계 | 선택한 편의시설 중 하나라도 없으면 제외됨 |
| 거리 | 동일 좌표 거리는 0km |
| 추천 | 커플 목적에서 감성·욕조·사우나 숙소가 우선됨 |
| 불변성 | 추천 정렬이 입력 배열과 객체를 변경하지 않음 |
| 미제공 필드 | 가격·인원·욕실을 임의 기본값으로 만들지 않고 명시적 필터에서 제외함 |
| 네이버 정규화 | 강조 태그/엔티티 제거, 좌표와 공식 필드 매핑 |
| 네이버 인증 | API HUB 엔드포인트, display=5, Client ID/Secret 헤더 사용 |
| 수집/중복 제거 | 여러 검색의 이름·좌표 중복 제거와 메타데이터 집계 |
| 보안 | 비 HTTP 링크 대체, 설정 오류에서 자격 증명 비노출 |
| API 서비스 | 제공자 상태, 공주 목록 응답, 503 공개 오류 변환 |
| 에어비앤비 | 데이터 수집 없이 공식 공주 검색 링크 생성 |
| API 예산 | 네이버 기본 질의 6개, 최대 30건, 별점 미제공 메타데이터 고정 |
| 화면 상태 | 목적 URL 전달, 허용되지 않은 목적 fallback, 제공자 연결 상태 표현 |
| 데이터 품질 | 공주시 외 주소와 숙박 외 업종 결과 제외 |
| 캐시 안전성 | 동시 미스를 단일 요청으로 병합하고 target별 결과 수를 정확히 투영 |
| 정적 파일 보안 | `.env`, `.git` 등 dotfile 경로를 공개 자산에서 차단 |
| 전체 공급자 장애 | 네이버 모든 질의 실패를 빈 성공 결과가 아닌 502 계열 오류로 변환 |

### 정적 검사

명령: `npm run check`

결과: **통과** — `app.js`, `connections.js`, `journey.js`, `provider-cache.js`, `static-files.js`, `domain.js`, `data.js`, `providers.js`, `api.js`, `server.mjs`의 Node 문법 오류 없음.

### 서버 자산 확인

로컬 서버에서 다음 경로의 HTTP 응답을 확인했습니다.

- `/`: 200
- `/src/app.js`: 200
- `/assets/stay-forest.svg`: 200
- `/explore.html?purpose=family`: 200
- `/connections.html`: 200
- `/.env`, `/.env.example`, `/.git/config`: 403
- `/api/providers`: 200, 네이버 `configured=true`, 에어비앤비 `outbound-search`
- `/api/gongju-listings?target=30`: 200, 질의 6회, 원본 30건, 중복 제거 후 27건, 오류 0건
- 동일 API 두 번째 요청: `cached=true`, 외부 API 추가 호출 없음
- `refresh=1` 우회 시도: `cached=true`, 최초 응답과 `fetchedAt` 동일

## 브라우저 기능 검증

| 시나리오 | 실제 결과 |
|---|---|
| 최초 로드 | 5개 출처, 9개 숙소 카드 렌더링 |
| 검색어 `평창` | 결과가 1곳으로 변경됨 |
| 존재하지 않는 지역 | 결과 0곳, 빈 결과와 초기화 버튼 표시 |
| 가족 여행 선택 | 추천 1위가 `가평 숲속 사우나 하우스`로 변경됨 |
| 낮은 가격순 | 1위가 `통영 작은 바다집`으로 변경됨 |
| 찜 | 카운트 1, `aria-pressed=true`, localStorage에 ID 저장 |
| 상세 보기 | 모달 제목과 데모 원문 링크 렌더링 |
| 제공자 상태 | 네이버 키 설정 필요와 에어비앤비 링크 모드를 화면에 표시 |
| 네이버 수집 버튼 | 키 미설정 시 `네이버 API 인증 설정이 필요합니다` 표시 후 버튼 복구 |
| 에어비앤비 링크 | `airbnb.co.kr` 공주 공식 검색 URL 설정 |
| 콘솔 | JavaScript 오류 및 콘솔 오류 0건 |
| 메인→가족 탐색 | URL 목적 `family`, 가족 목적 버튼 활성화, 샘플 카드 9개 렌더링 |
| 네이버 제한 수집 | 캐시 데이터 27건 렌더링, 카드 출처 전부 `네이버`, 별점 필터 아님 고지 |
| 연결 관리 | 로그인 `준비 중` 비활성, 네이버 공식 API와 에어비앤비 공식 링크 상태 구분 |

## 시각 검증

- 데스크톱에서 헤더, 목적 선택, 필터, 실데이터 연결 패널, 3열 카드, 안내/푸터의 겹침·잘림 없음
- 숙소 자체 제작 일러스트가 정상 표시됨
- Explore 표면에 맞게 필터와 결과 비교가 중심이며, 마케팅용 기능 카드 그리드를 사용하지 않음
- 반응형 CSS에서 900px 이하 2열, 620px 이하 1열 및 단일 열 모달 정의

### 디자인 슬롭 자가 점검

초기 점수: **0/10**

- 기술 그라데이션, 보라색 기본 강조, 기능 타일, 장식용 레일/블러/거대 수치/아이콘 토퍼 없음
- 중앙 정렬 스택이나 기본 Inter 서체를 사용하지 않음
- Explore 표면과 화면 구성이 일치함

수정 후 점수: **0/10** — 구성 결함이 발견되지 않아 별도 시각 수정 없음.

## 미검증/향후 검증

- 네이버 별점 필터: 공식 지역 검색 API가 별점 필드를 제공하지 않아 의도적으로 미구현
- 에어비앤비 API 데이터: 파트너 계약/Scope가 없어 의도적으로 미구현
- 가격/재고 정확도: 두 현재 연동 경로에서 제공하지 않아 미검증
- 실제 모바일 기기와 스크린리더 회귀 시험
- 다중 플랫폼 장애/지연을 가정한 어댑터 통합 시험
- 브라우저 간 호환성 매트릭스

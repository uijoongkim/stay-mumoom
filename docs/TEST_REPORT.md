# 테스트 보고서

검증일: 2026-09-05 KST

## 자동화 검증

### 단위 테스트

명령: `npm test`

결과: **48 passed, 0 failed**

| 대상 | 검증 내용 |
|---|---|
| 정규화 | 플랫폼 원본 필드가 공통 ID, 출처명, 가격, 욕실, 좌표로 변환됨 |
| 복합 필터 | 검색어, 최대 가격, 최소 욕실, 최소 인원, 최소 평점, 편의시설이 AND로 적용됨 |
| 편의시설 경계 | 선택한 편의시설 중 하나라도 없으면 제외됨 |
| 거리 | 동일 좌표 거리는 0km |
| 추천 | 커플 목적에서 감성·욕조·사우나 숙소가 우선됨 |
| 불변성 | 추천 정렬이 입력 배열과 객체를 변경하지 않음 |
| 미제공 필드 | 가격·인원·욕실을 임의 기본값으로 만들지 않고 명시적 필터에서 제외함 |
| 네이버 정규화 | 강조 태그/엔티티 제거, 좌표와 공식 필드 매핑 |
| 네이버 인증 | API HUB 엔드포인트, display=5, Client ID/Secret 헤더 사용 |
| 수집/중복 제거 | 여러 검색의 이름·좌표 중복 제거와 메타데이터 집계 |
| 보안 | 비 HTTP 링크 대체, 설정 오류에서 자격 증명 비노출 |
| API 서비스 | 제공자 상태, 전국 지역 목록 응답, 날짜·인원 전달, 503 공개 오류 변환 |
| 에어비앤비 | 데이터 수집 없이 입력 지역·날짜·인원의 공식 검색 링크 생성 |
| API 예산 | 네이버 기본 질의 6개, 최대 30건, 별점 미제공 메타데이터 고정 |
| 화면 상태 | 목적 URL 전달, 허용되지 않은 목적 fallback, 제공자 연결 상태 표현 |
| 데이터 품질 | 입력 지역 외 주소와 숙박 외 업종 결과 제외, 평점 없는 후보를 4.5 통과로 간주하지 않음 |
| 검색 조건 | 날짜 쌍 필수, 실제 달력/범위, 인원·평점·target의 10진수 문법·범위 엄격 검증, 행정 접미사·기호·emoji-only 지역 거부 |
| 가격 표시 | 천 단위 원화 표기, API 미제공 문구, 슬라이더 최댓값 `제한 없음` 표기 |
| 캐시 안전성 | 지역별 single-flight, settle 전후 in-flight eviction 보호, 임시 overcommit 후 상한 복원, target별 결과 수와 cached payload 불변성 |
| 비동기 UI | 요청 세대와 AbortController로 검색 중 입력 변경·초기화 시 이전 응답 무효화 |
| 요청 보호 | NAVER 글로벌 fetch 동시성 제한과 IP별 고정 창 요청률 제한기 |
| 정적 파일 보안 | `.env`, `.git` 등 dotfile 경로를 공개 자산에서 차단 |
| 전체 공급자 장애 | 네이버 모든 질의 실패를 빈 성공 결과가 아닌 502 계열 오류로 변환 |

### 정적 검사

명령: `npm run check`

결과: **통과** — 기존 파일과 새 `search.js`, `presentation.js`, `rate-limit.js`, `request-guard.js`를 포함한 전체 Node 소스의 문법 오류 없음.

### 서버 자산 확인

로컬 서버에서 다음 경로의 HTTP 응답을 확인했습니다.

- `/`: 200
- `/src/app.js`: 200
- `/assets/stay-forest.svg`: 200
- `/explore.html?purpose=family`: 200
- `/connections.html`: 200
- `/.env`, `/.env.example`, `/.git/config`: 403
- `/api/providers`: 200, 네이버 `configured=true`, 에어비앤비 `outbound-search`
- `/api/listings?region=제주 애월&checkin=2026-09-20&checkout=2026-09-22&guests=2&minRating=4.5&target=5`: 200
- NAVER 실 API: 질의 6회, 원본 27건, 지역/업종 제외 2건, 고유 후보 21건, 요청 반환 5건, 오류 0건
- 응답 메타: `ratingFilterApplied=false`, 요청 날짜·인원·최소 평점 보존
- Airbnb 링크: `제주 애월`, 체크인/체크아웃, 성인 2명 파라미터 반영
- 동일 지역 두 번째 요청: `cached=true`
- `target=0`, `target=1.5`, `target=0x10`, `guests=0x2`, `minRating=4.5e0`, 한쪽 날짜만 입력, 의미 없는 지역 `시`/`!!`: 각각 400
- 같은 IP의 `/api/listings` 1~10번째 요청 허용, 11번째 요청: 429

## 브라우저 기능 검증

| 시나리오 | 실제 결과 |
|---|---|
| 최초 로드 | 5개 출처, 9개 숙소 카드 렌더링 |
| 검색어 `평창` | 결과가 1곳으로 변경됨 |
| 존재하지 않는 지역 | 결과 0곳, 빈 결과와 초기화 버튼 표시 |
| 가족 여행 선택 | 추천 1위가 `가평 숲속 사우나 하우스`로 변경됨 |
| 낮은 가격순 | 1위가 `통영 작은 바다집`으로 변경됨 |
| 찜 | 카운트 1, `aria-pressed=true`, localStorage에 ID 저장 |
| 상세 보기 | 모달 제목과 데모 원문 링크 렌더링 |
| 제공자 상태 | 네이버 키 설정 필요와 에어비앤비 링크 모드를 화면에 표시 |
| 전국 지역 검색 | `제주 애월` 입력 후 NAVER 실 API 후보 21개 렌더링 |
| 날짜 입력 | 체크인/체크아웃 기본값 렌더링, 체크인 변경 시 체크아웃 최소일 갱신 |
| 가격 표시 | 샘플 `289,000원`, NAVER `가격 미제공 · 원문 확인`, 최댓값 `제한 없음` 표시 |
| 에어비앤비 링크 | `airbnb.co.kr`의 제주 애월 공식 검색 URL에 화면 날짜와 2명 설정 |
| 에어비앤비 독립 갱신 | NAVER 호출 전 지역 입력만으로 공식 링크 갱신, NAVER 결과와 독립 동작 |
| 실데이터 stale 방지 | 제주 검색 중 강릉 입력 시 signal 중단; fetch가 중단을 무시하고 늦게 성공해도 이전 제주 결과를 폐기하고 샘플 상태·강릉 Airbnb 링크 유지 |
| 선택 평점 안내 | 4.8 선택 시 결과 요약과 미검증 안내에 4.8을 표시 |
| 콘솔 | JavaScript 오류 및 콘솔 오류 0건 |
| 메인→가족 탐색 | URL 목적 `family`, 가족 목적 버튼 활성화, 샘플 카드 9개 렌더링 |
| 네이버 제한 수집 | 카드 출처 전부 `네이버`, `평점 미검증`, `사진 미제공`, 별점 필터 아님 고지 |
| 연결 관리 | 로그인 `준비 중` 비활성, 네이버 공식 API와 에어비앤비 공식 링크 상태 구분 |

## 시각 검증

- 데스크톱에서 헤더, 목적 선택, 필터, 실데이터 연결 패널, 3열 카드, 안내/푸터의 겹침·잘림 없음
- 샘플의 자체 제작 일러스트와 NAVER 실데이터의 `사진 미제공 · 공식 원문에서 확인` 상태가 서로 오인되지 않게 표시됨
- NAVER 후보 21개가 3열 그리드에서 겹침·잘림 없이 정렬되고 출처·평점 미검증·가격 미제공 문구가 일관되게 표시됨
- Explore 표면에 맞게 필터와 결과 비교가 중심이며, 마케팅용 기능 카드 그리드를 사용하지 않음
- 반응형 CSS에서 900px 이하 2열, 620px 이하 1열 및 단일 열 모달 정의

### 디자인 슬롭 자가 점검

초기 점수: **0/10**

- 기술 그라데이션, 보라색 기본 강조, 기능 타일, 장식용 레일/블러/거대 수치/아이콘 토퍼 없음
- 중앙 정렬 스택이나 기본 Inter 서체를 사용하지 않음
- Explore 표면과 화면 구성이 일치함

수정 후 점수: **0/10** — 구성 결함이 발견되지 않아 별도 시각 수정 없음.

## 미검증/향후 검증

- 네이버 별점 필터: 공식 지역 검색 API가 별점 필드를 제공하지 않아 의도적으로 미구현
- 에어비앤비 API 데이터: 파트너 계약/Scope가 없어 의도적으로 미구현
- 가격/재고 정확도: 두 현재 연동 경로에서 제공하지 않아 미검증
- 실제 모바일 기기와 스크린리더 회귀 시험
- 다중 플랫폼 장애/지연을 가정한 어댑터 통합 시험
- 브라우저 간 호환성 매트릭스
